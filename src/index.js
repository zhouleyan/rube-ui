import { Button, Input, Scroll, Select, Validator } from './module';

const components = {
  Button,
  Input,
  Scroll,
  Select,
  Validator
};

const Rube = {
  /* eslint-disable no-undef */
  version: __VERSION__,
  ...components
};

export default Rube;
